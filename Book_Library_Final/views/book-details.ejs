<!DOCTYPE html>
<html lang="en">
<head>
    <title><%= book.title %> - Details</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <%- include('partials/header') %>
    <div class="container">
        <div class="book-details">
            <div class="book-cover">
                <img src="https://covers.openlibrary.org/b/id/<%= book.coverId %>-L.jpg" alt="<%= book.title %> Cover">
            </div>
            <div class="book-info">
                <h1><%= book.title %></h1>
                <p><strong>Author:</strong> <%= book.author %></p>
                <p><strong>Genre:</strong> <%= book.genre %></p>
                <p><strong>Rating:</strong> <%= book.rating %>/5</p>
                <p><strong>Description:</strong> <%= book.description %></p>

                <% if (book.borrowLink) { %>
                    <a href="<%= book.borrowLink %>" target="_blank" class="btn">Borrow This Book</a>
                <% } else { %>
                    <p>Borrowing is not available for this book.</p>
                <% } %>

                <% if (book.buyLink) { %>
                    <a href="<%= book.buyLink %>" target="_blank" class="btn btn-buy">Buy This Book</a>
                <% } %>

                <% if (user) { %>
                    <form action="/users/reading-list/add/<%= book._id %>" method="POST">
                        <button type="submit" class="btn">Add to Reading List</button>
                    </form>
                <% } else { %>
                    <p><a href="/users/login" class="btn">Login to Add to Reading List</a></p>
                <% } %>
            </div>
        </div>
        <a href="/" class="btn btn-back">Back to Home</a>
    </div>
</body>
</html>




